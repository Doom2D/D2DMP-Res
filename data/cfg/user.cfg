name Player
color 64 255 64
skin default
bind Left +left
bind Right +right
bind Up +lookup
bind Down +lookdown
bind A +jump
bind S +attack
bind D +strafe
bind Q +nextweapon
bind Tab +scores
bind T +chat
